[package]
name = "od-vad"
description = "Voice activity detection: Silero v5 via ONNX Runtime, gating speech segments before STT."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
# ONNX Runtime bindings (ADR-4). Pinned exactly: 2.0 release candidates have
# breaking API churn between rcs.
ort = "=2.0.0-rc.10"
thiserror.workspace = true
tracing.workspace = true

[dev-dependencies]
# WAV loading for fixture-driven gate tests.
hound = "3"
