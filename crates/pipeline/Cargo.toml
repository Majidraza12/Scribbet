[package]
name = "od-pipeline"
description = "Session controller state machine, actor-per-stage orchestration, bounded channels, latency metrics."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
od-core-types.workspace = true
od-vad.workspace = true
od-stt.workspace = true
thiserror.workspace = true
tracing.workspace = true

[dev-dependencies]
# WAV loading for the fixture-driven end-to-end test.
hound = "3"
