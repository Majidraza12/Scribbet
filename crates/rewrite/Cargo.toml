[package]
name = "od-rewrite"
# Internal to this workspace; never published to crates.io.
publish = false
description = "Optional Rewriter trait. RulesRewriter passthrough is the default; Claude/OpenAI/local-LLM backends are feature-gated future plugins."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
od-core-types = { workspace = true }
