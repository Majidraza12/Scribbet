//! Rule-based text cleanup: TextProcessor trait and the ordered processor chain (whitespace, fillers, dictionary, symbols, punctuation, segmentation, capitalization, user rules, profiles).
//!
//! Scaffold only — implemented in its scheduled milestone (see /ROADMAP.md).
