{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Overlay window: event listening and the get_level command only. No fs, shell, or network APIs are exposed to the webview (docs/06-security.md TB2).",
  "windows": ["overlay"],
  "permissions": ["core:default"]
}
